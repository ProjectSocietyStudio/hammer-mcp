/**
 * Process running lives in `@projectsociety/mcp-core`, shared with gmod-mcp. Kept as a local
 * path so call sites do not care where it comes from.
 */
export { run, type RunOptions, type RunResult } from "@projectsociety/mcp-core";
